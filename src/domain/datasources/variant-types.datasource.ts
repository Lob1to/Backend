import { PaginationDto } from "../dtos";
import { VariantTypeEntity } from "../entities";

export abstract class VariantTypesDatasource {

    // abstract createVariantType(createVariantTypeDto: CreateVariantTypeDto): VariantTypeEntity;
    // abstract getVariantTypes(paginationDto: PaginationDto, getVariantTypesDto: GetVariantTypesDto): VariantTypeEntity[];
    // abstract getVariantType(id: number): VariantTypeEntity;
    // abstract updateVariantType(updateVariantTypeDto: UpdateVariantTypeDto): VariantTypeEntity;
    // abstract deleteVariantType(id: number): VariantTypeEntity;

}
